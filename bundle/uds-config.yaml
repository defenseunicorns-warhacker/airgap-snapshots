# Copyright 2024 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

# This single-cluster dev bundle deploys BOTH roles, so the peat peer config can't
# be expressed as package-level Zarf vars (both instances share the package name
# "snapback"). Each role's peer.id / peer.addr is set directly in the per-instance
# overrides in uds-bundle.yaml instead — nothing to configure here.
variables: {}
