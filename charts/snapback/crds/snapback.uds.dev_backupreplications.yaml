# Copyright 2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: backupreplications.snapback.uds.dev
spec:
  group: snapback.uds.dev
  names:
    kind: BackupReplication
    listKind: BackupReplicationList
    plural: backupreplications
    shortNames:
    - brep
    singular: backupreplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.backupName
      name: Backup
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.objectCount
      name: Objects
      type: integer
    - jsonPath: .status.totalBytes
      name: Bytes
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupReplication is the unit of work and audit for replicating
          one Backup.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupReplicationSpec is the desired replication of one Velero
              Backup.
            properties:
              backupName:
                description: BackupName is the Velero Backup to replicate.
                type: string
              policyName:
                description: PolicyName is the ReplicationPolicy governing this replication.
                type: string
              storageLocation:
                description: StorageLocation is the Velero BackupStorageLocation the
                  backup lives in.
                type: string
            required:
            - backupName
            - policyName
            - storageLocation
            type: object
          status:
            description: BackupReplicationStatus is the observed replication state.
            properties:
              bundles:
                description: Bundles tracks per-bundle progress.
                items:
                  description: BundleStatus tracks one SendAttachments bundle.
                  properties:
                    bundleID:
                      description: BundleID is the deterministic peat bundle_id for
                        idempotent re-sends.
                      type: string
                    bytes:
                      description: Bytes is the total declared bytes in this bundle.
                      format: int64
                      type: integer
                    bytesTransferred:
                      description: BytesTransferred is the sender-side served byte
                        count.
                      format: int64
                      type: integer
                    distributionIDs:
                      description: DistributionIDs are the peat distribution IDs (one
                        per file).
                      items:
                        type: string
                      type: array
                    fileCount:
                      description: FileCount is the number of FileSpecs in this bundle.
                      format: int32
                      type: integer
                    phase:
                      description: Phase is the bundle's current state.
                      enum:
                      - Pending
                      - InProgress
                      - Completed
                      - Failed
                      - Cancelled
                      type: string
                  required:
                  - bundleID
                  - bytes
                  - fileCount
                  - phase
                  type: object
                type: array
              bytesTransferred:
                description: BytesTransferred is the sender-side served byte count
                  across bundles.
                format: int64
                type: integer
              completedAt:
                description: CompletedAt is when all bundles reached COMPLETED.
                format: date-time
                type: string
              conditions:
                description: Conditions are detailed observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastError:
                description: LastError is the most recent error message, if any.
                type: string
              objectCount:
                description: ObjectCount is the number of BSL objects in this replication's
                  delta.
                format: int64
                type: integer
              observedGeneration:
                description: ObservedGeneration is the last spec generation reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level lifecycle phase.
                enum:
                - Pending
                - Staging
                - Transmitting
                - Replicated
                - Failed
                - Skipped
                type: string
              retryCount:
                description: RetryCount is the number of reconcile-driven re-sends.
                format: int32
                type: integer
              startedAt:
                description: StartedAt is when staging began.
                format: date-time
                type: string
              totalBytes:
                description: TotalBytes is the total bytes to transfer for this delta.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
